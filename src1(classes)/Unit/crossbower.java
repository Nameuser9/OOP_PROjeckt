package Unit;

public class crossbower extends units {

    int shootdmg;
    int ammo;

    public crossbower(int maxHealth, int healthPoints, int movePoints, boolean range, int defence, int attack, int dmg, int init, int price) {
        super(maxHealth, healthPoints, movePoints, range, defence, attack, dmg, init, price);
    }

    public String shoot() {
        return ("в разработке)");
    }
}